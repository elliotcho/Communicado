Convo - [MessageBubbles]
[MsgCards]
SendMsg

Global state:
Recipients

SendMsg
chatId will exist or 'new'

if(new): create 
make message card with convo active
add one new message card 
render the Convo

else: message in chat
if user is looking at that convo real time add bubble (emit read, so that others looking at chat saw you read)
if user isn't on msg page color navbar
if user is not on msg card bold text in card with new msg
update message cards order


                     routes   <-- view/client
                        |
db <--> models <--> controllers
                        |
                      socket  <-- view/client